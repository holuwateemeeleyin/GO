package main

func main() {
	//Maps are colection of key-value pairs that a user can use to store some data and retrieve it with a key
	// In Contrast to an array/slice, finding an entry in a map requires a single lookup versus iterating over the entire slice comparing values.
	//With a large set of Items, this can save time.
}
